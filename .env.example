# App URL (important when project is in a subfolder)
APP_BASE_URL=https://test.kstaq.de/guniee

# Database configuration
# Set DB_DRIVER to mysql on all-inkl production
DB_DRIVER=sqlite

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=guineedortmund2026
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# SQLite (used only when DB_DRIVER=sqlite)
DB_PATH=database/dev.sqlite
